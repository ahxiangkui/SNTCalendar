# 二年级单位换算试题生成器

## 功能说明

这个工具可以自动生成适合二年级学生的单位换算练习题，输出为Word文档格式。

### 特点

1. **格式精美**：生成的Word文档格式类似普通试卷，包含标题、姓名栏、班级栏等
2. **题量充足**：默认生成10页，每页约50题
3. **题目类型丰富**：
   - 长度单位换算：米 ↔ 厘米
   - 时间单位换算：时 ↔ 分 ↔ 秒
   - 人民币单位换算：元 ↔ 角
4. **两种题型**：
   - 单位换算填空题（约70%）
   - 比大小题（约30%）

## 安装依赖

```bash
pip install -r requirements.txt
```

## 使用方法

### 基本使用

直接运行脚本，将生成默认配置的试题：

```bash
python generate_unit_conversion_test.py
```

这将生成名为 `二年级单位换算试题.docx` 的文件，包含10页，每页50题。

### 自定义配置

你也可以在Python代码中自定义参数：

```python
from generate_unit_conversion_test import create_test_paper

# 生成自定义配置的试题
create_test_paper(
    filename="我的单位换算试题.docx",  # 自定义文件名
    pages=5,                          # 生成5页
    questions_per_page=40             # 每页40题
)
```

## 题目示例

### 填空题

- 5米 = (    )厘米
- 300厘米 = (    )米
- 3时 = (    )分
- 4分 = (    )秒
- 8元 = (    )角
- 50角 = (    )元

### 比大小题

- 3米 ○ 250厘米
- 2时 ○ 100分
- 5元 ○ 60角

## 输出文件

生成的Word文档包含：

- 页面标题：二年级数学单位换算练习题
- 学生信息栏：姓名、班级、日期、得分
- 两个大题：
  - 一、单位换算填空题（每题2分）
  - 二、比大小（在○里填上">"、"<"或"="）（每题2分）

## 技术说明

- 使用 `python-docx` 库生成Word文档
- 随机生成题目，确保每次生成的试题都不同
- 支持中文字体（宋体、黑体）
- 自动分页处理

## 依赖项

- Python 3.6+
- python-docx 0.8.11

## 注意事项

- 确保系统已安装中文字体（宋体、黑体）
- 生成的Word文档可以用Microsoft Word、WPS等软件打开编辑
- 题目难度适合小学二年级学生

## 许可证

本工具遵循仓库主项目的许可证。
